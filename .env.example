# ══════════════════════════════════════════════════════════════════════════════
# FILE INI HANYA UNTUK DEVELOPMENT / RUN LOKAL TANPA DOCKER
# Di production (Docker), semua env diinjek dari docker-compose.yml
# yang membaca apisimak/.env secara otomatis.
# ══════════════════════════════════════════════════════════════════════════════

# ── Database (sama dengan apisimak/.env) ──────────────────────────────────
DB_HOST=103.124.198.68
DB_PORT=3306
DB_NAME=db_simpeg
DB_USER=adminsimak
DB_PASSWORD=BKPSDM2025

# ── Service ───────────────────────────────────────────────────────────────
FACE_SERVICE_HOST=127.0.0.1
FACE_SERVICE_PORT=8001

# ── ArcFace threshold (0.0–1.0) ───────────────────────────────────────────
ARCFACE_THRESHOLD=0.40

# ── Upload foto (Tahap 2/Opsi B: HTTP ke endpoint internal apisimak) ──────
APISIMAK_INTERNAL_URL=http://127.0.0.1:8080/api/internal/foto
APISIMAK_INTERNAL_TOKEN=

# ── HANYA untuk scripts/extract_embeddings.py (bulk, baca file lokal) ─────
FOTO_FACE_PATH=../simak/uploads/foto_face
